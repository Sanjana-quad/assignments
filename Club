/* 1. Body styling */
body {
    background-color: #00FFFF;
    font-family: Arial, sans-serif;
    font-size: 12px;
}

/* 2. H1 styling */
h1 {
    font-size: 2.5em;
}

/* 3. dd element styling */
dd {
    font-size: 1.5em;
}

/* 4. Paragraph with a title attribute */
p[title] {
    color: #FF0000;
    font-size: 1.7em;
    line-height: 1.5;
}

/* 5. Class "reasons" styling */
.reasons {
    color: #FF0F70;
    font-weight: bold;
    line-height: 1.5;
    font-size: 1.5em;
}

/* 6. Class "description01" styling */
.description01 {
    font-style: italic;
    color: #F500FF;
}

/* 7. Class "description02" styling */
.description02 {
    font-style: italic;
    color: #FF4500;
    font-weight: bold;
}

/* 8. Emphasis element styling */
em {
    color: #FF0000;
    font-weight: bold;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS Styling Example</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
<body>

    <h1>CSS Styling Example</h1>

    <p>This page demonstrates various CSS selectors and their corresponding styles.</p>

    <dl>
        <!-- dd Element Selector Explanation -->
        <dt>Selector: dd</dt>
        <dd>This selector applies font size of 1.5em to definition descriptions.</dd>
    </dl>

    <!-- Paragraph with Title Attribute -->
    <p title="example">This is a paragraph with a title attribute, styled with the attribute selector.</p>

    <!-- Class Selector: reasons -->
    <p class="reasons">The reasons class is styled with bold text, pink color, and 1.5em font size.</p>

    <!-- Class Selector: description01 -->
    <p class="description01">The description01 class has italic text and purple color.</p>

    <!-- Class Selector: description02 -->
    <p class="description02">The description02 class has italic, bold text with orange color.</p>

    <!-- Emphasis Tag Selector -->
    <p>This sentence contains <em>emphasized text</em> which is styled in bold and red color.</p>

</body>
</html>
